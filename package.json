{
  "name": "jokic",
  "version": "1.0.0",
  "description": "An aggregated view of every NBA metric for every NBA player",
  "main": "server.js",
  "scripts": {
    "api:start": "nodemon ./api/src/server.js",
    "ui:start": "cd ui && yarn vite",
    "ui:build": "cd ui && yarn vite build",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs --fix --ignore-path .gitignore",
    "start": "yarn api:start && yarn ui:build",
    "start-dev": "yarn api:start && yarn ui:start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.15.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
